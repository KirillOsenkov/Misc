<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:CardsSample">

  <DataTemplate DataType="{x:Type local:Card}">
    <StackPanel Margin="16">
      <Image Source="{Binding Image}" Width="128" Height="128" />
      <TextBlock Text="{Binding Title}" FontSize="24" Foreground="White" />
    </StackPanel>
  </DataTemplate>

  <DataTemplate DataType="{x:Type local:Category}">
    <StackPanel Margin="24">
      <TextBlock Text="{Binding Name}" FontSize="24" Foreground="LightYellow" />
      <ListBox ItemsSource="{Binding Cards}" BorderThickness="0" Background="Transparent">
        <ListBox.ItemsPanel>
          <ItemsPanelTemplate>
            <StackPanel Orientation="Horizontal" IsItemsHost="True" />
          </ItemsPanelTemplate>
        </ListBox.ItemsPanel>
      </ListBox>
    </StackPanel>
  </DataTemplate>

  <DataTemplate DataType="{x:Type local:Library}">
    <ItemsControl ItemsSource="{Binding Categories}">
      <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
          <StackPanel IsItemsHost="True" />
        </ItemsPanelTemplate>
      </ItemsControl.ItemsPanel>
    </ItemsControl>
  </DataTemplate>

</ResourceDictionary>